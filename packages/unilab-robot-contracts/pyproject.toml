[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "unilab-robot-contracts"
version = "0.1.0"
description = "Uni-Lab 厂商无关机械臂命令、观测与执行后端合同"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
